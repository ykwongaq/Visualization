"""yk_visualization — Computer vision visualization utilities.

Subpackages:
    bbox        — Draw bounding boxes on images.
    segmentation — Overlay or crop segmentation masks.
    depth       — False-color depth map visualization.
    point       — Draw labeled point overlays on images.
    image_grids — Composite images into grid layouts.
    video       — Video / frame conversion utilities.
"""
